# Phase 0 cleanup ledger

This ledger records implementation and contract inconsistencies as they are
verified. Authorship is not evidence of a defect. The evidence table retains
the initial action note recorded when each finding was discovered. Those notes
are noncanonical and may contain alternatives; the single current decision in
the canonical registry below supersedes every initial note.

| ID | Finding | Reachability/evidence | Current impact | Initial action note (superseded) |
|---|---|---|---|---|
| CL-01 | `InputData` documentation says “two variants” although the enum has `Transmission`, `Counts`, and `CountsWithNuisance`. | `crates/nereids-pipeline/src/pipeline.rs:205-247` | Misstates the public Rust data contract. | correct documentation; route disposition pending |
| CL-02 | `SolverConfig` says invalid combinations are unrepresentable, but `CountsWithNuisance + LM` is representable and rejected at runtime. | `crates/nereids-pipeline/src/pipeline.rs:265-292,1322-1327` | Overstates type-level safety. | correct documentation or redesign types after compatibility review |
| CL-03 | Raw-count documentation says it “always uses Poisson KL,” while explicit LM performs a ratio/Gaussian fallback. The fallback ignores `c`, open-beam variance, and attached alpha-fit config. | `crates/nereids-pipeline/src/pipeline.rs:225-229,1302-1320,1359-1389,2600-2613`; E04 reproduces ignored `c`, and E10 shows single F4 accepting `fit_alpha_1` but returning no alpha fields. | Violates the raw-count API contract whenever exposures differ, understates uncertainty, and can silently drop requested nuisance coordinates. | deprecate F4/S4 and reject incompatible config before conversion |
| CL-04 | One `PoissonKL` enum/parser label selects two distinct objectives: legacy single-arm Poisson NLL for transmission and joint-Poisson conditional-binomial deviance for counts. The Python aliases `poisson` and `joint_poisson` are also accepted for transmission. Transmission KL ignores the supplied uncertainty during optimization and release mode accepts a negative observed transmission. | `crates/nereids-pipeline/src/pipeline.rs:270-290,1241-1258`; `bindings/python/src/lib.rs:4390-4441`; `crates/nereids-fitting/src/poisson.rs:1-22,133-139`; the Phase 0 semantics probe changed relative uncertainty weights by 1e8 and obtained bit-identical KL density while LM changed, then accepted `T=-0.1` and reported convergence. | Solver names do not identify the statistic, and the transmission route violates both its normalized-data uncertainty contract and the Poisson observation domain. | deprecate F2/S2 as a scientific route; reserve `joint_poisson` for raw-count data and provide a migration warning |
| CL-05 | Python single and spatial APIs expose `detector_background`, `fit_alpha_1`, and `fit_alpha_2`, but behavior varies among hard rejection, silent F4 omission, Python-spatial binding rejection, and an accepted S5 all-failed/NaN result. | `bindings/python/src/lib.rs:4680-5059,5203-5511`; `docs/guide/src/python-api.md:197-209,272`; `crates/nereids-pipeline/src/pipeline.rs:1704-1736`; `crates/nereids-pipeline/src/spatial.rs:1807-1839,1954-2042`; E10 | Public API promises one nuisance feature while exposing four incompatible failure/ignore semantics. | hide the controls until one typed two-arm likelihood and one rejection contract exist |
| CL-06 | Comments say `CountsWithNuisance` lets callers supply detector background, immediately before the production fitter rejects any nonzero value. | `crates/nereids-pipeline/src/pipeline.rs:1260-1268,1704-1736` | Contradictory source-level contract. | correct once F5 disposition is fixed |
| CL-07 | Fixed-flux `CountsModel` and `CountsBackgroundScaleModel` are public Rust types used by the research Fisher helper and tests, with a different flux convention from production joint-Poisson. | public module `crates/nereids-fitting/src/lib.rs:29`; types/scope `crates/nereids-fitting/src/poisson.rs:18-20,1151-1164,1253-1263`; helper uses `crates/nereids-pipeline/src/pipeline.rs:3576-3605` | Duplicate counts semantics are available to Rust consumers and are reachable through the research helper, not the production fit route. | rename and isolate as fixed-flux research API |
| CL-08 | The dedicated resolution calibrator profiles `a·model + constant + linear-in-bin-index`, whereas production science routes use SAMMY energy functions and/or a multiplicative log-energy baseline. | `crates/nereids-fitting/src/resolution_calib.rs:1-17,637-682`; `crates/nereids-pipeline/src/pipeline.rs:37-49,117-148` | Calibration parameters can absorb a nuisance model that is not transferred to science fits. | keep only as legacy option; add aligned calibration route before interpreting IC physically |
| CL-09 | The current shared production forward model applies resolution after Beer–Lambert to transmission (`R[T]`); raw-count joint-Poisson then scores that same-bin transmission rather than forming migrated open/sample arms separately. | `crates/nereids-fitting/src/transmission_model.rs:512-549`; `crates/nereids-pipeline/src/pipeline.rs:1894-1900` | Counts route is statistically count-native but not response-migration-native. | keep as compatibility baseline; Phase 1 replacement requires counts-specific response operator |
| CL-10 | Python accepts IC in production fits only after converting it to `TabulatedResolution`; Rust can carry a native `ResolutionFunction::IkedaCarpenter`. | `bindings/python/src/lib.rs:767-846,2019-2045`; production fit signatures accept `Option<PyTabulatedResolution>` | The analytical IC object is not preserved across the Python fit boundary, limiting IC-specific optimization/derivatives. | `keep+improve`; expose a common resolution union if performance work needs native IC identity |
| CL-11 | Spatial raw-count KL replaces every pixel's open beam with one spatial mean and passes that mean as the joint-Poisson `O` observation without representing the averaging variance/exposure. | `crates/nereids-pipeline/src/spatial.rs:215-228,1649-1704,1773-1805`; synthetic gradient probe produced false densities `0…6.30e-4` for true `8e-5`, while paired input recovered truth to `1.56e-10` relative; real VENUS OB diagnostic measured 4.55% nonzero-pixel total CV versus 1.06% shot CV. | Beam/pixel-efficiency structure can become a fully “converged” false density structure; the available real fixture is not uniform at percent precision. | retain only as an explicitly approximate averaged-OB mode; make paired per-pixel counts the scientific default and return assumption diagnostics |
| CL-12 | `CountsWithNuisance.flux` is passed as `JointPoissonObjective.o`—a Poisson-observed open arm—not as a deterministic flux estimate as its name/docs imply. | `crates/nereids-pipeline/src/pipeline.rs:1286-1300,1894-1900`; `crates/nereids-pipeline/src/spatial.rs:227-228` | Ambiguous uncertainty semantics; callers cannot tell whether input variance is modeled. | rename to paired open-beam counts or implement a distinct known-flux likelihood |
| CL-13 | Python stubs claim detector background is available for the LM-converted counts path; providing it constructs `CountsWithNuisance`, which LM rejects. | `bindings/python/python/nereids/__init__.pyi:1787`; `crates/nereids-pipeline/src/pipeline.rs:1322-1327` | Static API guidance directs users to an impossible combination. | correct stub immediately; F4 remains a deprecation candidate |
| CL-14 | `compute_model_jacobian` is described as sharing `fit_counts_spectrum_typed` semantics, but uses fixed-flux Fisher, a legacy two-term background, supports alpha fitting rejected in production, rejects multiplicative baseline, and has no `c`. | `bindings/python/python/nereids/__init__.pyi:1893`; `crates/nereids-pipeline/src/pipeline.rs:3333-3434,3562-3611` | Research identifiability results can be mistaken for the production objective's information geometry. | rename/isolate as research-only and build a production-objective Jacobian before using it for calibration decisions |
| CL-15 | `PyFitResult` commentary retains the removed counts layout `[b0,b1,alpha2]`, while production counts results use SAMMY `[BackA,BackB,BackC]`. | `bindings/python/src/lib.rs:411-417`; `crates/nereids-pipeline/src/pipeline.rs:3762-3777` | Stale binding maintenance contract. | correct documentation and add result-layout binding test |
| CL-16 | `UnifiedFitConfig` documentation says unspecified counts polish is enabled for a single spectrum, while `JointPoissonFitConfig::default()` is now disabled everywhere. | `crates/nereids-pipeline/src/pipeline.rs:397`; `crates/nereids-fitting/src/joint_poisson.rs:776-789` | Performance/optimizer behavior differs from documented defaults. | correct documentation; retain explicit opt-in polish |
| CL-17 | MCP single-spectrum dispatch defaults to LM and normalizes counts unless the manifest explicitly selects the counts fit domain, unlike the typed API's counts→KL `Auto` rule. | `bindings/python/python/nereids/mcp/server.py:664-711` | Surface-dependent default can silently change the likelihood. | align MCP default with typed data-domain dispatch or require an explicit domain |
| CL-18 | Only Gaussian resolution gets an automatic extended/fine auxiliary grid. Tabulated UDR and IC operate on the caller grid, truncate/renormalize at edges, and depend on external margin plus `fit_energy_range`; GUI adds a margin but the Python API does not, and transmission KL rejects the mask. | `crates/nereids-physics/src/transmission.rs:37-100,360-380`; `crates/nereids-physics/src/resolution.rs:2010-2022,2092-2134`; `crates/nereids-pipeline/src/pipeline.rs:478-497,1563-1577`; `apps/gui/src/guided/analyze.rs:1968-2068` | Identical resolution data can produce surface/window-dependent edge behavior. | add a shared all-family working-grid/margin contract; another reason to retire transmission KL |
| CL-19 | Direct Rust IC and Python `ic.as_tabulated()` can select different acceleration routes: scalar/cubature surrogates gate on `Tabulated`, excluding native IC. | `crates/nereids-fitting/src/transmission_model.rs:245,380`; Python conversion at `bindings/python/src/lib.rs:843-849` | Same physical kernel can have API-representation-dependent performance/approximation behavior. | unify resolution capability dispatch before IC optimization |
| CL-20 | Python `background=True` silently enables free Anorm+A+B+C and exposes no A/B/C per-term init/free controls, although Rust supports them. | `BackgroundConfig::default`, `crates/nereids-pipeline/src/pipeline.rs:66-99`; binding `bindings/python/src/lib.rs:6162-6178` | Main user API cannot perform controlled background identifiability experiments or reproduce selective SAMMY cards. | expose explicit term controls or offer named constrained presets |
| CL-21 | Free density + temperature + Anorm is known degenerate but only emits a warning; active-bin validation checks parameter count, not rank/condition number. | `crates/nereids-pipeline/src/pipeline.rs:2418-2438,2584-2659` | A numerically converged result may remain structurally non-identifiable. | add Jacobian-rank/profile gate and require priors/frozen coordinates for production calibration |
| CL-22 | Spatial validation rejects invalid BackD/F starts, but single-spectrum validation checks only paired flags. With `back_d_init=0` and both flags true, the documented invariant says the BackF Jacobian is identically zero, yet the single-spectrum boundary has no corresponding value check. | `crates/nereids-pipeline/src/spatial.rs:941-970`; `crates/nereids-pipeline/src/pipeline.rs:2291-2319`; parameter invariant documented at `crates/nereids-pipeline/src/pipeline.rs:60-68` | Surface-dependent validation permits an unidentifiable configuration to reach the single-spectrum optimizer. | harmonize validation before retaining D/F as supported |
| CL-23 | Result/docs say BackD/F are LM-transmission-only, while transmission Poisson constructs and returns the exponential wrapper. | `crates/nereids-pipeline/src/pipeline.rs:1623-1644,1669-1692,3779-3792`; `crates/nereids-pipeline/src/spatial.rs:99-112`; `bindings/python/src/lib.rs:1049-1053` | Stale feature matrix and untested KL behavior. | remove with transmission KL or test/document during its deprecation window |
| CL-24 | Ordinary energy-scale fitting always frees t0 and L_scale together, with fixed boxes and no prior or option to pin one coordinate; spatial transmission permits this per pixel without a real-data gate. | `crates/nereids-pipeline/src/pipeline.rs:2249-2288`; `crates/nereids-pipeline/src/spatial.rs:880-905` | Adds the same timing/kernel degeneracy that the dedicated calibrator warns against. | experimental only; add independent priors/freeze controls and shared calibration before production use |
| CL-25 | Rust single-spectrum and spatial boundaries validate different input/config invariants: Rust config can start with nonfinite/negative densities; single transmission values are not validated; LM silently gives invalid σ negligible weight, whereas spatial rejects it. | `crates/nereids-pipeline/src/pipeline.rs:529-595,1177-1232`; `crates/nereids-fitting/src/parameters.rs:31-80`; `crates/nereids-fitting/src/lm.rs:674-695`; spatial validation `crates/nereids-pipeline/src/spatial.rs:562-714`; Python density validation `bindings/python/src/lib.rs:6078-6095` | The same invalid data/config can error, be ignored, or “converge” depending on entry point. | create one shared boundary validator and path-parity tests |
| CL-26 | E11 now covers ordinary Python production fits for IC converted to `TabulatedResolution` on F1 and F3. Remaining integration gaps are native Rust IC, ordinary-fit UDR, spatial IC/UDR, noisy recovery, and real calibration transfer; the existing tabulated spatial smoke uses unresolved generated data. | E11 in `investigation/evidence/phase-0/test-results.md`; native-IC/unit and coverage-gap inventory in E08; `crates/nereids-pipeline/src/spatial.rs:2427-2542` | The new IC-as-tabulated smoke closes one handoff cell, but kernel unit tests and that matched noise-free probe do not establish native-family parity or calibration transfer. | complete remaining native IC/UDR, spatial, noisy, and real-transfer coverage before claiming resolution-family parity |
| CL-27 | Resolution calibration accepts only normalized transmission+uncertainty and profiles WSSR; there is no raw sample/open-beam likelihood aligned with the primary science route. | `crates/nereids-fitting/src/resolution_calib.rs:637-682,799-806`; production domains at `crates/nereids-pipeline/src/pipeline.rs:205-247` | Resolution parameters can absorb the calibration/science likelihood mismatch. | preserve as legacy transmission calibrator; add response-aware count calibration before production IC transfer |
| CL-28 | Calibration `fit_background` profiles unbounded normalization+constant+linear-in-bin-index, discards the fitted coefficients, and differs from both SAMMY and multiplicative production backgrounds. | profiler/result `crates/nereids-fitting/src/resolution_calib.rs:526-575,637-682` | Nuisance absorption is invisible and not reproducible in downstream science fits. | return/profile coefficients and align the nuisance model; retain old form only as labeled legacy |
| CL-29 | Calibration can fit t0/L with priors, but returned position is not directly pinnable through production fit APIs; bounds also differ (calibration L 0.98–1.02 versus production 0.99–1.01), and flat-prior fits remain allowed despite warnings. | `crates/nereids-fitting/src/resolution_calib.rs:444-521,966ff`; `crates/nereids-pipeline/src/pipeline.rs:2249-2288`; Python result `bindings/python/src/lib.rs:1324-1347` | A calibrated energy scale cannot be transferred as the same constrained quantity and may be silently refit. | unify position object/bounds and add pin/prior transfer plus corrected-energy helper |
| CL-30 | Calibration diagnostics omit nuisance coefficients, Jacobian/SVD, covariance/correlation, profiles, per-restart outcomes, component timing, and even `n_evals`, though Nelder–Mead computes it. | `crates/nereids-fitting/src/nelder_mead.rs:75`; `crates/nereids-fitting/src/resolution_calib.rs:526-575,1075-1228` | Convergence/bound hits cannot establish identifiability or quantify uncertainty/performance. | add diagnostics before physical parameter interpretation; bound hits alone are insufficient |
| CL-31 | Every calibration objective evaluation rebuilds the resolution and reruns the full fixed-sample forward model; a small selected IC convergence test took 115.81 s. | `crates/nereids-fitting/src/resolution_calib.rs:1084-1115`; exact E09 command/result | Verified performance bottleneck at route level, not merely notebook overhead. | cache invariant nuclear/Doppler work and benchmark only after continuous kernel fixes |
| CL-32 | UDR correction preserves the base table's stored flight path, but calibrator config has a separate flight path and does not enforce equality. | `crates/nereids-physics/src/resolution.rs:948ff`; `crates/nereids-fitting/src/resolution_calib.rs:591-596` | Width correction/calibration can combine inconsistent TOF↔energy geometry. | reject mismatch and persist provenance/hash |
| CL-33 | Calibrator's IC parameterization is a bounded subset (sqrt-E alpha, constant R, beta, optional PSR; burst Gaussian disabled), while public IC construction supports more laws/components. | `crates/nereids-fitting/src/resolution_calib.rs:598-629`; `crates/nereids-physics/src/ikeda_carpenter.rs:274ff`; Python constructor `bindings/python/src/lib.rs:764ff` | “IC calibration” can be misread as calibrating every IC capability. | name the exact family/coordinates and require model-selection evidence before expanding |
| CL-34 | Calibrated kernel deployment/persistence is half-wired: no pipeline/GUI calibration call, GUI persists Gaussian scalars or a tabulated file path, and runtime tabulated output has no save/export method. | GUI `apps/gui/src/widgets/design.rs:852`; project `crates/nereids-io/src/project.rs:110`; Python in-memory handoff `bindings/python/src/lib.rs:1407-1425` | A successful calibration cannot be reproducibly stored and transferred with complete provenance. | add embedded/checksummed resolution artifact and project schema before calling calibration deployable |
| CL-35 | Tabulated kernels silently use the nearest reference outside synthesis range, and handoff does not enforce a compatible science energy range. | `crates/nereids-physics/src/resolution.rs:2439ff`; calibration handoff `bindings/python/src/lib.rs:1407-1425` | Out-of-range science fits can appear valid while extrapolating kernel shape by clamping. | persist/enforce validity range or synthesize a declared extended grid |
| CL-36 | The calibration example is stale: its cached IC output shows only a0/a1, while the current result returns beta/r/PSR. The runtime and stub PSR defaults are both 350 ns and are not a mismatch. | `examples/notebooks/workflows/06_resolution_calibration.ipynb:150-168`; result decoding `bindings/python/src/lib.rs:1350-1385`; runtime signature `bindings/python/src/lib.rs:1490-1497`; stub `bindings/python/python/nereids/__init__.pyi:762-783`; shared default `crates/nereids-fitting/src/resolution_calib.rs:122` | The cached example does not reproduce the current parameter/result contract even though the live signature and stub agree. | regenerate and execute the example after the calibration API disposition |
| CL-37 | Gaussian calibration hard-codes the exponential-tail coordinate to zero although lower-level Gaussian resolution APIs support it. | `crates/nereids-fitting/src/resolution_calib.rs:585-589`; public tail-capable `crates/nereids-physics/src/resolution.rs:162-254,533-741` | The family label suggests broader Gaussian-resolution parity than is calibrated. | relabel C1 as symmetric-Gaussian calibration |
| CL-38 | Calibration starts are fixed by family; restarts shift every coordinate in the same positive-bound direction. There is no user seed or space-filling multistart. | `crates/nereids-fitting/src/resolution_calib.rs:362ff,1075-1090` | A curved/ridged objective can report a local basin as the calibration without basin diversity evidence. | physics-derived starts plus space-filling multi-starts and clustered/profiled solutions |
| CL-39 | GUI HDF5 sample-only modes explicitly wrap raw counts as `Transmission` with uniform σ=1; the GUI default solver is PoissonKL, so a reachable default applies the legacy fractional-transmission NLL to raw count magnitudes. | `apps/gui/src/guided/normalize.rs:525-617`; default `apps/gui/src/state.rs:1613-1625`; dispatch `apps/gui/src/guided/analyze.rs:2422-2440` | Invalid data-domain interpretation can produce a numerical “fit” instead of demanding an open beam. | block HDF5 raw counts without OB from fitting; require an explicit pre-normalized input type for transmission-only files |
| CL-40 | GUI normalized transmission uses `proton_charge_sample/proton_charge_ob`, while counts joint-Poisson uses independent mutable `kl_c_ratio`. | `apps/gui/src/guided/normalize.rs:437-462`; `apps/gui/src/guided/analyze.rs:2313-2321` | Displayed/normalized data and fitted raw-count likelihood can use different exposure ratios. | one provenance-backed exposure-ratio source shared by normalization, fit, overlay, and result metadata |
| CL-41 | Spatial counts fitting uses averaged open beam, but GUI overlays scale fitted T by each pixel's local `c·OB`. | `crates/nereids-pipeline/src/spatial.rs:1649-1704`; `apps/gui/src/guided/analyze.rs:1399-1416` | Overlay is not necessarily the forward prediction whose objective produced the parameters. | return the effective modeled arms/route provenance and render those exact predictions |
| CL-42 | MCP single raw-count workflow defaults to `solver=lm` and a hidden transmission fit domain; MCP spatial counts defaults through Python Auto to joint-Poisson. | single `bindings/python/python/nereids/mcp/server.py:640-717`; spatial `bindings/python/python/nereids/mcp/server.py:780-1044` | The same manifest-level “counts” concept uses different likelihoods by mode. | deprecate the hidden single default and use typed Auto/F3 |
| CL-43 | MCP `fit_domain` is a free string and any value other than exact `counts` silently selects transmission; solver/domain validation does not reject typos or incompatible pairs. | `bindings/python/python/nereids/mcp/server.py:684-711,1051ff`; injected route probe | A typo can silently switch the statistical engine. | enum/schema validation with explicit rejection and effective-route output |
| CL-44 | MCP fit allowlists omit valid production controls (baseline, fit_anorm, density freeze/masks, χ² scaling, detector background) and its validator accepts incomplete/unknown fit-resolution combinations. | `bindings/python/python/nereids/mcp/server.py:480-551,1051ff` | Legitimate configuration is silently dropped while invalid-looking configuration can pass validation. | generated schema from the typed core config; reject unknown keys rather than drop them |
| CL-45 | GUI and MCP expose no resolution-calibration or IC-construction workflow, and calibrated tabulated output has no public provenance-preserving export. | GUI `ResolutionMode` has only Gaussian/tabulated variants at `apps/gui/src/state.rs:582-593`; MCP resolution dispatch has only none/Gaussian/tabulated at `bindings/python/python/nereids/mcp/server.py:442-467`; calibration is bound only through Python at `bindings/python/src/lib.rs:1490-1676`; its in-memory handoff is `bindings/python/src/lib.rs:1407-1425`; project resolution persistence is scalar/path-only at `crates/nereids-io/src/project.rs:110-116` | Calibration is callable only in Python/Rust memory and cannot flow reproducibly through other supported surfaces. | add persistence first, then surface calibration consistently |
| CL-46 | Counts joint-Poisson stores deviance/dof in the legacy reduced-χ² field; several GUI locations still label it χ² although the detailed panel distinguishes it. | result adapter `crates/nereids-pipeline/src/pipeline.rs:1695-1702`; stale map summary `apps/gui/src/guided/results.rs:54-75`; stale fit feedback `apps/gui/src/guided/analyze.rs:2458-2467,2653-2662`; distinguishing detail panel `apps/gui/src/guided/analyze.rs:1891-1907` | Users can compare or interpret unlike GOF metrics as the same statistic. | typed GOF enum/value and surface-parity tests |
| CL-47 | MCP reports transport-level `success=true` for an unconverged fit, drops structured warnings, and omits effective-route provenance. | MCP fit summary reads convergence but not warnings/route at `bindings/python/python/nereids/mcp/server.py:592-629`; wrapper sets unconditional success at `bindings/python/python/nereids/mcp/server.py:1575-1592`; core structured warnings are exposed at `bindings/python/src/lib.rs:628-634` | Automation can treat a failed inference or degenerate fit as successful. | success must require declared convergence policy; return warnings and resolved route |
| CL-48 | Spatial `n_failed` counts thrown per-pixel errors, not nonconverged fits. | per-pixel `Err` increments at `crates/nereids-pipeline/src/spatial.rs:1834-1839`; nonconverged `Ok` results are skipped without increment at `crates/nereids-pipeline/src/spatial.rs:1954-1962`; counters are returned at `crates/nereids-pipeline/src/spatial.rs:2040-2042` and copied into MCP at `bindings/python/python/nereids/mcp/server.py:1039-1042` | A map can have few “failed” pixels but many scientifically unusable nonconverged pixels. | separate error, nonconverged, masked/dead, and converged counts |
| CL-49 | Python resolution calibration maps all failures to `ValueError`; energy calibration may return a best finite nonconverged result without exposing convergence. | resolution-calibration error mapping `bindings/python/src/lib.rs:1673-1675`; energy-calibration binding/result fields `bindings/python/src/lib.rs:3466-3486`; finite-χ² rather than LM-convergence acceptance `crates/nereids-pipeline/src/calibration.rs:880-910,986-1029` | Callers cannot distinguish invalid configuration from numerical failure or enforce success. | typed error taxonomy and convergence fields across calibration APIs |
| CL-50 | `UnifiedFitConfig::with_counts_background` is unconditional: F1/F2/S1/S2 silently ignore the config; Rust F4/S4 silently ignore alpha flags; Python single F4 reaches that omission while Python spatial rejects earlier. | config/builder `crates/nereids-pipeline/src/pipeline.rs:369-395,739-742`; dispatch/omission `crates/nereids-pipeline/src/pipeline.rs:1241-1320,2600-2613`; Python split `bindings/python/src/lib.rs:5041-5045,5455-5487`; E10 | A caller can request counts nuisance behavior and receive a successful fit that never used it, with surface-dependent validation. | reject counts-background config on every incompatible domain/solver boundary |

## Canonical cleanup disposition registry

Each cleanup ID appears exactly once here with one allowed disposition. The
action is the implementation decision; no alternative in the superseded notes
overrides it.

| ID | Assigned disposition | Required action |
|---|---|---|
| CL-01 | **keep+improve** | Keep all three input variants and correct the stale two-variant documentation. |
| CL-02 | **keep+improve** | Keep the runtime rejection and correct the claim that invalid solver/data combinations are unrepresentable. |
| CL-03 | **deprecate** | Deprecate F4/S4, reject unequal exposure and alpha config, and migrate raw counts to F3/S3/S5 or normalized transmission to F1/S1. |
| CL-04 | **deprecate** | Deprecate F2/S2 and reserve joint-Poisson naming for raw-count objectives. |
| CL-05 | **complete before exposure** | Hide detector-background/alpha controls until one nonnegative two-arm likelihood passes Q2/Q7 and all disabled paths satisfy Q9 rejection parity. |
| CL-06 | **complete before exposure** | Rewrite the current comments as paired-open-beam semantics and remove every claim that nonzero detector background already works. |
| CL-07 | **keep+improve** | Rename and isolate the fixed-flux model as research-only with its objective and flux convention in the public type name/metadata. |
| CL-08 | **keep+improve** | Label the index-linear calibration nuisance legacy, return its coefficients, and add a count-aligned calibration option. |
| CL-09 | **keep+improve** | Preserve current same-bin `R[T]` as a versioned compatibility operator and add separate migrated open/sample arms. |
| CL-10 | **keep+improve** | Expose one typed Python resolution union so native IC identity is retained through production fits. |
| CL-11 | **keep+improve** | Rename S3 as averaged-open-beam approximation, publish its flatness diagnostic, and make paired S5-style input the scientific default. |
| CL-12 | **complete before exposure** | Rename the current field to paired open-beam counts; any future known-flux likelihood must use a distinct input type. |
| CL-13 | **remove** | Remove the impossible LM detector-background claim from the stub and solver choices. |
| CL-14 | **keep+improve** | Isolate and rename the Fisher helper as fixed-flux research code and implement a separate production-objective Jacobian. |
| CL-15 | **remove** | Delete the obsolete counts result-layout comment and replace it with the actual ABC layout plus a binding test. |
| CL-16 | **remove** | Delete the stale default-polish statement and document the existing explicit opt-in behavior. |
| CL-17 | **deprecate** | Deprecate the MCP hidden transmission/LM default and resolve raw-count manifests through typed counts Auto/F3. |
| CL-18 | **keep+improve** | Implement one all-family working-grid, margin, and validity-range contract. |
| CL-19 | **keep+improve** | Unify native/tabulated capability and acceleration dispatch before IC optimization. |
| CL-20 | **keep+improve** | Expose per-term ABC controls and retain the Boolean shortcut only as a named constrained preset. |
| CL-21 | **keep+improve** | Add Jacobian-rank and two-sided profile gates before a converged result is treated as identifiable. |
| CL-22 | **keep+improve** | Apply the same finite/positive D/F start validation at every single and spatial boundary. |
| CL-23 | **deprecate** | Retire transmission-KL D/F behavior with F2/S2 and test the warning during the migration window. |
| CL-24 | **complete before exposure** | Keep ordinary t0/L fitting experimental until either coordinate can be pinned or independently constrained and transfer-tested. |
| CL-25 | **keep+improve** | Create one shared boundary validator and identical invalid-input tests across single, spatial, Rust, and Python paths. |
| CL-26 | **complete before exposure** | Add native-IC, true-UDR, spatial, noisy, and real-transfer integrations before claiming resolution-family parity. |
| CL-27 | **keep+improve** | Preserve transmission-WSSR as a labeled legacy calibrator and add a response-aware raw-count calibrator. |
| CL-28 | **keep+improve** | Return and bound profiled nuisance coefficients and persist their basis; retain the old form only as labeled legacy. |
| CL-29 | **keep+improve** | Unify t0/L objects and bounds and add pin/prior transfer plus corrected-energy output. |
| CL-30 | **complete before exposure** | Return nuisance, evaluation-count, Jacobian/SVD, covariance/profile, restart, and timing diagnostics before physical interpretation. |
| CL-31 | **keep+improve** | Cache invariant nuclear/Doppler work only after parity tests, then meet the fixed calibration/map benchmark protocol. |
| CL-32 | **keep+improve** | Reject UDR/config flight-path mismatch and persist the matching path and base-table checksum. |
| CL-33 | **keep+improve** | Name the exact bounded IC family/coordinates in APIs and results and keep other laws out until model-selection gates pass. |
| CL-34 | **complete before exposure** | Add an embedded checksummed resolution artifact and project round trip before exposing deployment workflows. |
| CL-35 | **keep+improve** | Persist and enforce the tabulated kernel validity range instead of silently clamping outside it. |
| CL-36 | **remove** | Remove the stale cached notebook output and regenerate/execute the example against the retained calibration API. |
| CL-37 | **keep+improve** | Relabel C1 as symmetric-Gaussian calibration; treat any tail family as a separate future cell requiring independent evidence. |
| CL-38 | **keep+improve** | Replace one-direction restarts with user-seeded space-filling starts and report clustered/profiled solutions. |
| CL-39 | **remove** | Block GUI raw-HDF5 fitting without an open beam; accept transmission-only files only through an explicitly normalized type. |
| CL-40 | **remove** | Collapse proton-charge and KL-ratio state into one checked, persisted exposure ratio. |
| CL-41 | **keep+improve** | Return effective modeled arms and render those exact predictions with averaged-OB provenance. |
| CL-42 | **deprecate** | Deprecate MCP single-count hidden LM/transmission dispatch and use the same typed Auto rule as spatial/core paths. |
| CL-43 | **remove** | Replace free-string fallback with strict domain/solver enums that reject unknown values. |
| CL-44 | **remove** | Replace silent hand-written allowlists with a generated typed schema that rejects unknown or unsupported fields. |
| CL-45 | **complete before exposure** | Implement checksummed resolution persistence before adding GUI/MCP calibration or native-IC controls. |
| CL-46 | **remove** | Remove overloaded chi-square labels/fields and replace them with a typed GOF statistic. |
| CL-47 | **remove** | Remove unconditional MCP success and return separate execution, convergence, warnings, and resolved-route fields. |
| CL-48 | **remove** | Replace `n_failed` with explicit error, nonconverged, masked/dead, and converged counters. |
| CL-49 | **remove** | Replace all-`ValueError`/hidden convergence semantics with typed calibration errors and explicit convergence fields. |
| CL-50 | **remove** | Reject counts-background config on transmission and LM-count routes instead of silently ignoring it. |

## Test coverage and explicit gaps

Evidence IDs refer to the registry and exact-command sections in
`investigation/evidence/phase-0/test-results.md`. “Static-only” means the source
mechanism was verified but no passing sibling route is counted as coverage; the
missing acceptance test is stated explicitly.

| ID | Direct test coverage or explicit gap |
|---|---|
| CL-01 | Static-only documentation finding; missing a rustdoc/API-contract check that enumerates all three `InputData` variants. |
| CL-02 | E10 directly exercises and rejects `CountsWithNuisance + LM`; missing a compile-time/API-shape test for any redesign that makes the combination unrepresentable. |
| CL-03 | E04 directly reproduces ignored `c`, nonconvergence, and the manual pre-scaling control; E07 measures stochastic fallback bias; E10 proves F4 accepts and omits `fit_alpha_1` and reproduces the S4 failure. |
| CL-04 | E04 directly verifies uncertainty invariance and negative-transmission acceptance for single-spectrum transmission KL; E10 repeats the negative-observation behavior for spatial KL. |
| CL-05 | E10 verifies zero-background recovery, single hard rejections, F4 silent omission, Python-spatial alpha rejection, and S5's accepted all-failed/NaN result; the positive nonzero-background Q2/Q7 route is missing because wiring does not exist. |
| CL-06 | E10 directly verifies the nonzero detector-background rejection; missing a positive detector-background route test because production wiring does not exist. |
| CL-07 | E06 runs the fixed-flux helper/Fisher tests; exact type and helper-use sources above establish repository reachability. External downstream use is not knowable from this checkout; a Q4 convention-parity test against production joint-Poisson is missing. |
| CL-08 | E09 exercises the calibrator background profiler; E08 exercises production SAMMY background/baseline paths separately; missing a calibration-to-science nuisance-transfer parity test. |
| CL-09 | E02, E03, and E11 exercise the current same-bin `R[T]` joint-Poisson path on real and matched synthetic data; missing a migrated-open/sample-arm simulation that discriminates the response operator. |
| CL-10 | E11 directly exercises Python IC-to-tabulated handoff on F1/F3; missing native-IC versus tabulated representation parity, derivative, and dispatch tests. |
| CL-11 | E05 directly reproduces the false density gradient and paired-open-beam control; E12 measures the real open-beam nonuniformity without fitting it. |
| CL-12 | E05 exercises `CountsWithNuisance.flux` as the paired open observation; missing a discriminating known-flux-versus-observed-open-arm likelihood test. |
| CL-13 | E10 directly verifies that supplying nuisance data with LM is rejected; missing a stub/runtime contract test that prevents the unsupported combination from being advertised. |
| CL-14 | E06 exercises `evaluate_jacobian_and_fisher`; missing numerical Jacobian/Fisher parity against the production joint-Poisson objective, including `c`, baseline, and detector background. |
| CL-15 | Static-only result-layout mismatch; missing a Python binding test asserting `[BackA, BackB, BackC]` semantics for counts results. |
| CL-16 | E13 directly runs `test_enable_polish_kwarg_accepted_and_toggles_behavior`; missing a default-resolution test that locks documentation to the actual disabled default. |
| CL-17 | E13's durable MCP route probe directly verifies the no-fit-block LM/transmission default; live FastMCP execution remains skipped and is an explicit gap. |
| CL-18 | E08 covers Gaussian auxiliary grids, resolution plans, and fit ranges; E13 covers transmission-KL fit-range rejection; missing all-family edge-domain parity across Rust, Python, and GUI. |
| CL-19 | E08 covers native IC kernel/plan units and E11 covers IC-as-tabulated production fits; missing a same-kernel native-versus-tabulated acceleration/accuracy benchmark. |
| CL-20 | E08 covers production background behavior and E13 covers background/baseline rejection; missing Python tests for per-term A/B/C initialization/freezing because those controls are not exposed. |
| CL-21 | Static-only identifiability finding; missing a rank/condition/profile acceptance test for free density+temperature+Anorm. |
| CL-22 | Static-only validation asymmetry; missing a direct single-spectrum regression proving rejection of `back_d_init=0` with both D/F flags enabled. |
| CL-23 | Explicitly untested behavior: missing transmission-KL BackD/F recovery, result-layout, and rejection-window tests. |
| CL-24 | E08 runs ordinary energy-scale tests; missing one-coordinate pin/prior controls and a real spatial energy-scale transfer gate. |
| CL-25 | E08/E13 cover several individual validation paths; missing a path-parity matrix that feeds identical invalid density, transmission, and uncertainty inputs to Rust single, Python single, and spatial boundaries. |
| CL-26 | E11 passes matched noise-free IC-as-tabulated F1/F3 recovery; E08 covers native IC only at kernel/plan level and records the unresolved tabulated spatial smoke. Missing native IC, ordinary-fit UDR, spatial IC/UDR, noisy recovery, and real calibration-transfer tests. |
| CL-27 | E09 exercises normalized-transmission WSSR calibration; missing a raw sample/open-beam calibration likelihood and a matched count-calibration recovery test. |
| CL-28 | E09 passes `calibrate_with_background_runs` and `inner_chi2_background_path_and_degenerate_model`; missing returned-coefficient and downstream nuisance-transfer parity tests. |
| CL-29 | E09 passes injected t0 recovery, prior-penalty, and flat-L diagnostics; missing a pin/prior round-trip from calibration result into ordinary production fits. |
| CL-30 | E09 tests current result/boundary fields; missing schema and numerical tests for nuisance coefficients, Jacobian/SVD, covariance/correlation, profiles, restart outcomes, evaluation counts, and component timing. |
| CL-31 | E09 directly reruns the selected IC convergence test: 1 passed in 115.81 s; missing component-level profiling and a parity-guarded optimized benchmark. |
| CL-32 | E09 passes UDR width/exponent recovery; missing a mismatched-base/config-flight-path rejection and matching-path acceptance test. |
| CL-33 | E09 covers the bounded calibration IC family and one selected convergence case; missing model-selection tests against the additional public IC energy laws/components. |
| CL-34 | Static-only deployment/persistence finding; missing calibrated-kernel save/load provenance round-trip and GUI/project calibration handoff tests. |
| CL-35 | E08 covers tabulated-kernel behavior and E11 covers an in-range handoff; missing an out-of-validity-range rejection/export-range test. |
| CL-36 | E09 verifies the current Python result fields; source inspection proves the runtime and stub defaults are both 350 ns. The notebook cache was not executed in Phase 0. Missing an executed-notebook/API-drift gate. |
| CL-37 | E09 passes symmetric-Gaussian width recovery; missing a public family-label test that explicitly excludes the tail coordinate from C1. |
| CL-38 | E09 includes calibrator cases with multiple restarts; missing seed control, basin-diversity, and clustered-solution tests. |
| CL-39 | Static-only GUI reachability finding; E13 explicitly records that no GUI interaction was exercised. Missing an HDF5 sample-only default-fit integration test that must reject raw counts without open beam. |
| CL-40 | Static-only GUI configuration finding; missing an exposure-ratio provenance/parity test spanning normalization, counts fitting, overlays, and saved results. |
| CL-41 | E05 validates the averaged-open-beam model effect; GUI overlay behavior is static-only under E13. Missing a GUI overlay-versus-effective-forward-prediction parity test. |
| CL-42 | E13 directly verifies the MCP single default; the precisely cited spatial path is source-only and FastMCP was skipped. Missing a live single-versus-spatial Q9 manifest parity test. |
| CL-43 | E13's durable route probe directly verifies that a misspelled fit domain selects transmission; missing live schema-level rejection because FastMCP was unavailable. |
| CL-44 | E13 runs the symbol drift check and source/helper inspection; missing unknown-key, omitted-valid-key, and incompatible fit/resolution validation tests through live MCP. |
| CL-45 | E13 records source inspection only, with FastMCP skipped and no GUI exercise; missing GUI/MCP calibration-surface tests plus a provenance-preserving export/import round-trip. |
| CL-46 | E06 exercises counts deviance production paths; GUI labels were inspected statically under E13. Missing GUI/project surface tests that assert `D/dof` for counts and `chi2_r` only for Gaussian objectives. |
| CL-47 | E13 covers helper/source routing but not a live server result. Missing a live unconverged-fit test asserting transport failure policy, warnings propagation, and effective-route provenance. |
| CL-48 | Static-only counter-semantics finding; missing a spatial result/MCP summary test containing both thrown-error pixels and nonconverged `Ok` pixels. |
| CL-49 | E09 covers selected resolution-calibration boundary errors; missing typed error-class tests and an energy-calibration finite-but-nonconverged result test with an exposed convergence field. |
| CL-50 | E10 directly proves Python F4 silently omits alpha and Python S4 rejects; F1/F2/S1/S2 and Rust S4 omissions are source-only. Missing Q9 rejection tests for every incompatible route/config pair. |
